import styled from 'styled-components'
import { Layout } from '@/components/Layout'
import { COLORS } from '@/theme/theme.default'

const LayoutStyled = styled(Layout)`
  box-shadow: 0 0 3px ${COLORS.GRAYSCALE_1};
`

export { LayoutStyled }
